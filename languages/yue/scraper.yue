export search =
    title: 'Search'
    subtitle: 'Search results'
    handler: (query, progress) ->
        {}

export layers =
    * title: 'Layer'
      handler: (media, progress) ->
        {}

export actions =
    * title: 'Stream'
      description: 'Stream description'
      handler: (medias, progress) ->

    * title: 'Download'
      description: 'Download description'
      handler: (medias, progress) ->
